# To-Clarify.md

> 用于记录 Agent 当前无法处理、阻塞 Plan 继续拆解的外部问题
> 问题被澄清后，结果写入对应 Plan 节点，然后从本文件删除

## 待澄清问题

### 高优先级

1. **P3.1.2 解锁：Magii 后端 API 可用性**：P3.1.2（后端 API 对接层）是 P3.1 的关键路径瓶颈。P3.1.3 和 P3.1.4 均依赖此节点。需确认后端 API 的接口契约、访问方式与可用时间。
   - 影响：P3.1 关键路径阻塞，P3.1.3/P3.1.4 无法推进
   - 解锁动作：确认后端 API 接口契约与可用状态

2. **P2.2 解锁：Roadmap 数据结构约束**：P2.2（Roadmap 接入契约定义）处于 BLOCKED 状态，解锁条件为外部流程提供 Roadmap 的数据结构约束（Roadmap schema 文档或示例数据文件）。
   - 影响：P2.2 无法推进，进而 P2 无法完成
   - 解锁动作：由持有 Roadmap 的人（外部流程）提供 Roadmap 所使用的 Plan 数据格式

### 中优先级

1. **P0 与 P1 语义对齐**：当前 P0 表达为"AI Agent 工作流标准定义框架"（源自产品方向），而 P1 描述的是"智能调度与注册中心系统"（源自初始化计划）。两者是否同义？若 Magii 的 root goal 本质上就是"构建具备自我学习与进化能力的智能调度注册系统"，则 P0.name 应更新以与之对齐；若"框架"是更宽泛的愿景，则 P1 只是 P0 的一个子集实现。
   - 影响：P0 名称、Vision 陈述是否需要修订
   - 建议：在下一个过程中用 Lens 显式对齐

### 低优先级

## 风险假设
